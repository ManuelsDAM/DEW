{
  "name": "lol-champions-app",
  "version": "1.0.0",
  "description": "League of Legends Champions App con Parcel",
  
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "clean": "rm -rf dist .parcel-cache",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": ["league-of-legends", "api", "champions", "parcel"],
  "author": "Tu nombre",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.9.0"
  },
  "browserslist": [
    "last 2 versions"
  ]
}